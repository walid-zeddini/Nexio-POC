version: '2'
services:
  nexio-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/NEXIO/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=NEXIO
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
